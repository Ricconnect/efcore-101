{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Connection string for the local Docker SQL Server instance.
    // Start the database with: docker compose up -d
    // This will be used once Entity Framework Core is introduced.
    "DefaultConnection": "Server=localhost,1433;Database=MedicineManager;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;"
  }
}
